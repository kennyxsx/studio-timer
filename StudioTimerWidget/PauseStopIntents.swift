// StudioTimerWidget/PauseStopIntents.swift
//
// The Live Activity buttons in `StudioTimerLiveActivity` use `Link(destination:)`
// with the `studio-timer://` URL scheme, NOT App Intents. We kept this file
// around in case we revisit App Intents for Shortcuts integration in the
// future. Empty for now.
import Foundation
